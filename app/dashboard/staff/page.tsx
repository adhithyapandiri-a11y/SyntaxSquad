'use client'; import React, { useState } from 'react';
import { Wrench, CheckSquare, Clock, CheckCircle2, Upload, AlertCircle, Sparkles } from 'lucide-react';
import { Card, CardHeader, CardTitle, CardContent } from '@/components/ui/Card';
import { Button } from '@/components/ui/Button';
import { Badge } from '@/components/ui/Badge';
import { Modal } from '@/components/ui/Modal';
import { useStore } from '@/lib/store/useStore';
import { formatDateTime } from '@/lib/utils'; export default function StaffDashboardPage() { const { complaints, updateComplaintStatus } = useStore(); const [selectedComplaint, setSelectedComplaint] = useState<any | null>(null); const [resolutionNotes, setResolutionNotes] = useState(''); const [completionPhoto, setCompletionPhoto] = useState(''); const handleResolveSubmit = (e: React.FormEvent) => { e.preventDefault(); if (!selectedComplaint) return; updateComplaintStatus( selectedComplaint.id, 'resolved', 'stf_1', 'Robert Vance', resolutionNotes || 'Fixed successfully. Tested operation.', completionPhoto || 'https://images.unsplash.com/photo-1581092160607-ee22621dd758?w=500' ); setSelectedComplaint(null); setResolutionNotes(''); }; return ( <div className="space-y-6"> {/* Staff Header */} <div className="flex flex-col md:flex-row md:items-center justify-between gap-4 p-6 rounded-2xl bg-white border border-[rgba(0,0,0,0.06)] shadow-[0_2px_8px_rgba(0,0,0,0.02)] text-[#0A0A0A] "> <div> <div className="flex items-center gap-2"> <span className="px-2.5 py-0.5 rounded-full bg-[#FAFAFA] text-[#0A0A0A] text-xs font-bold uppercase"> Maintenance Staff Workspace </span> <span className="text-xs text-[#757575]">Robert Vance • HVAC Lead</span> </div> <h1 className="text-2xl sm:text-3xl font-extrabold tracking-[-0.05em] mt-1"> Assigned Work Orders & Repair Jobs </h1> <p className="text-sm text-[#757575] mt-1"> Update work order status, upload repair proof photos, and resolve student complaints. </p> </div> </div> {/* Complaints Kanban / List for Staff */} <div className="grid grid-cols-1 md:grid-cols-2 gap-6"> {/* Active Work Orders */} <Card className="bg-white border border-[rgba(0,0,0,0.06)] rounded-2xl p-6"> <CardHeader> <CardTitle className="flex items-center gap-2 text-[#0A0A0A] tracking-[-0.05em]"> <Clock className="w-5 h-5 text-[#0A0A0A]" strokeWidth={1.5} /> Active Maintenance Orders ({complaints.filter(c => c.status !== 'resolved').length}) </CardTitle> </CardHeader> <CardContent className="p-0 divide-y divide-[rgba(0,0,0,0.06)] "> {complaints.filter(c => c.status !== 'resolved').map((cmp) => ( <div key={cmp.id} className="p-5 space-y-3"> <div className="flex items-start justify-between"> <div> <span className="text-xs font-bold px-2 py-0.5 rounded bg-[rgba(0,0,0,0.04)] text-[#0A0A0A] capitalize"> {cmp.category} </span> <h4 className="text-sm font-bold text-[#0A0A0A] mt-1">{cmp.title}</h4> <p className="text-xs text-[#757575]">Room {cmp.roomNumber} • Student: {cmp.studentName}</p> </div> <Badge variant={cmp.priority === 'high' ? 'danger' : 'warning'}>{cmp.priority}</Badge> </div> <p className="text-xs text-[#757575] ">{cmp.description}</p> {cmp.estimatedFixTime && ( <p className="text-xs font-semibold text-[#0A0A0A] "> ⏱️ Target Fix Time: {cmp.estimatedFixTime} </p> )} <div className="flex items-center justify-end gap-2 pt-2 border-t border-[rgba(0,0,0,0.06)] "> <Button size="sm" variant="outline" onClick={() => updateComplaintStatus(cmp.id, 'in_progress')} > Mark In Progress </Button> <Button size="sm" variant="success" onClick={() => setSelectedComplaint(cmp)} > <CheckCircle2 className="w-4 h-4 mr-1" strokeWidth={1.5} /> Complete & Resolve </Button> </div> </div> ))} </CardContent> </Card> {/* Work History */} <Card className="bg-white border border-[rgba(0,0,0,0.06)] rounded-2xl p-6"> <CardHeader> <CardTitle className="flex items-center gap-2 text-[#0A0A0A] tracking-[-0.05em]"> <CheckSquare className="w-5 h-5 text-[#0A0A0A]" strokeWidth={1.5} /> Completed Repairs Log </CardTitle> </CardHeader> <CardContent className="p-0 divide-y divide-[rgba(0,0,0,0.06)] "> {complaints.filter(c => c.status === 'resolved').map((cmp) => ( <div key={cmp.id} className="p-4 space-y-2"> <div className="flex items-center justify-between"> <h4 className="text-xs font-bold text-[#0A0A0A] ">{cmp.title}</h4> <Badge variant="success">Resolved</Badge> </div> <p className="text-xs text-[#757575]">Room {cmp.roomNumber} • Resolved on {formatDateTime(cmp.resolvedAt || cmp.createdAt)}</p> {cmp.resolutionNotes && ( <p className="text-xs text-[#0A0A0A] font-medium">Notes: {cmp.resolutionNotes}</p> )} </div> ))} </CardContent> </Card> </div> {/* Completion Modal */} {selectedComplaint && ( <Modal isOpen={!!selectedComplaint} onClose={() => setSelectedComplaint(null)} title="Mark Job Order as Resolved" subtitle={`Upload completion photo & resolution note for ${selectedComplaint.title}`} > <form onSubmit={handleResolveSubmit} className="space-y-4"> <div> <label className="block text-xs font-bold uppercase tracking-wider text-[#757575] mb-1"> Resolution Summary & Technician Notes </label> <textarea required rows={3} value={resolutionNotes} onChange={(e) => setResolutionNotes(e.target.value)} placeholder="e.g. Replaced compressor valve and recharged refrigerant gas. Tested cooling." className="w-full px-3 py-2 text-sm bg-[#FAFAFA] border border-[rgba(0,0,0,0.06)] rounded-xl focus:outline-none " /> </div> <div> <label className="block text-xs font-bold uppercase tracking-wider text-[#757575] mb-1"> Completion Photo URL (Optional) </label> <input type="url" value={completionPhoto} onChange={(e) => setCompletionPhoto(e.target.value)} placeholder="Paste proof image URL or use default" className="w-full px-3 py-2 text-sm bg-[#FAFAFA] border border-[rgba(0,0,0,0.06)] rounded-xl focus:outline-none " /> </div> <div className="flex justify-end gap-2 pt-3 border-t border-[rgba(0,0,0,0.06)] "> <Button type="button" variant="ghost" onClick={() => setSelectedComplaint(null)}>Cancel</Button> <Button type="submit" variant="success">Submit Completion Report</Button> </div> </form> </Modal> )} </div> );
}
